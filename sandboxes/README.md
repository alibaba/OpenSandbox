# Sandbox Image

Here are the base images we will use inside the sandbox container.

## Code Interpreter

The official image provided from OpenSandbox, which is able to execute multiple language like node/python, etc.

## Sandbox Base

Custom Image migrated from code-interpreter that installed claude code with wrapper http server in it.
